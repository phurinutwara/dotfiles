- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf

- create:
    - ~/Works

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
