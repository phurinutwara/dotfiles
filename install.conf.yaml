- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf

- create:
    - ~/Works

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
